# 更新 0.14.20 发布边界

- 将发布说明同步到当前 `main` 已完成的 Component 类型闭包：Unit 结果、Bool、Buffer、Number、String、递归 List 与闭合单态 Option/Result。
- 明确 Calcit 继续只生成 Canonical ABI core module；runnable Component 由独立 `calcit-bindgen` 生成，并已通过 Wasmtime 与 jco/Node 验证。
- 把公开 `cr-wasm` 的退役版本修正为 0.14.20，避免发布包内容与升级文档不一致。
- Struct、普通 Enum、明确宽度数值与 async 仍保留为后续边界，不在发布说明中宣称完成。
