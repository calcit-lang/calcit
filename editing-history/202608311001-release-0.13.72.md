# 发布 0.13.72 / Release 0.13.72

- 发布近期 FFI review 收尾：canonical WIT Unit-result forms、目标规范化后的生成标识符冲突守门，以及 native base symbol ABI 后缀拒绝。
- 同步 `Cargo.toml`、`package.json` 与 workspace lockfile 版本。

- Release the recent FFI review closure: canonical WIT Unit-result forms, generated-identifier collision guards after target normalization, and native base-symbol ABI suffix rejection.
- Synchronize versions in `Cargo.toml`, `package.json`, and the workspace lockfile.
