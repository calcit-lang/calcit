# 在 writer lock 中校验质量基线

CodeRabbit 指出原先的 `--write-baseline` 在校验旧文件后才获取原子写入锁，两个 CLI 写入者可能在校验与提交之间互相覆盖。现在先调用现有 staging 机制获取该目标文件的 writer lock，再读取和比较旧 baseline，持锁直到原子替换或错误回滚完成。

回归测试覆盖锁竞争、拒绝新建文件、写入失败不改旧内容、预算下降与旧格式迁移。此保护适用于遵守仓库现有 writer lock 协议的写入者；外部不遵守协议直接修改同一文件的进程仍需由项目工作流协调。
