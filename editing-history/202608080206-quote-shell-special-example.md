# Quote shell-special definition names in documentation

- Wrap the `detect-nodejs?` definition argument in single quotes in the JavaScript example-checking command.
- This prevents shells from interpreting the trailing question mark as a glob pattern.
