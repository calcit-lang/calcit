# Release 0.13.70 / 发布 0.13.70

- Release deterministic multi-target FFI bindgen previews from Interface IR v1.
- Source exported package versions from `deps.cirru` manifests.
- Validate native symbols, invocation modes, versioned transports, backend
  targets, and protocol pairings before bindgen.

- 发布基于 Interface IR v1 的确定性多目标 FFI bindgen preview。
- 从 `deps.cirru` manifest 读取导出 package version。
- 在 bindgen 前校验 native symbol、invoke、版本化 transport、backend target
  与协议组合。
