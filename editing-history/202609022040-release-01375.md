# Release 0.13.75

2026-09-02 20:40 CST

## English

- Release the nominal trait reference matching fix from PR #593.
- Match resolved `Trait` annotations with qualified `TypeRef` schemas and source-backed generic trait bounds without conflating same-named traits from different namespaces.
- Unblock strict type validation across Respo, alerts, and gen-code on a published compiler baseline.

## 中文

- 发布 PR #593 中的 nominal trait 引用匹配修复。
- 让已解析的 `Trait` annotation 与限定名 `TypeRef` schema、带 source identity 的泛型 trait bound 正确匹配，同时不混淆不同 namespace 中的同名 trait。
- 为 Respo、alerts 和 gen-code 的严格类型验证提供正式发布的编译器基线。
