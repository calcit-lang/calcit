# Nil / Unit documentation review

- Corrected the static-analysis type table after the runtime/type split.
- `Unit` is represented by `&unit` and no longer accepts `nil`.
- `Nil` is documented as a distinct legacy absence type.
